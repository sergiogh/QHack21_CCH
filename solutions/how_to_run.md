To run the coded solution and pass it the inputs, use the scheme:
`python ./simple_circuits_20_sol.py < QML_Challenges/simple_circuits_20_template/1.in

**NOTE:**
Just use the packages under `requierements.txt`.
